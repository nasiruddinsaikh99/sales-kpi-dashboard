<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /sales-kpi-dashboard/
    
    # Send all requests that are not existing files or folders to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
